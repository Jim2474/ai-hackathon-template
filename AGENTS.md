# Hackathon AI Coding Agents Guide

## 当前目标

这是一个 AI Coding 黑客松项目。最高目标不是写出完美工程，而是在限定时间内交付一个可运行、可演示、可解释的 MVP。

优先级：

1. 可运行
2. 主流程完整
3. Demo 能讲清楚
4. UI 清楚
5. 代码优雅

## 用户情况

用户不是专业程序员。回答时必须用小白能懂的话说明：

- 要运行什么命令；
- 要打开哪个页面；
- 要点击哪里；
- 看到什么结果说明成功；
- 如果失败，要复制哪段报错。

不要默认用户理解前端、后端、接口、数据库、依赖、构建等概念。

## 工作原则

1. 先实现最小闭环，不要一开始做完整系统。
2. 如果后端、数据库、真实 API 太复杂，优先使用 mock 数据、localStorage、静态 JSON。
3. 每次只完成一个明确目标，不要一次性大改项目。
4. 修改代码前先查看项目结构。
5. 新增依赖前必须说明理由。
6. 遇到报错时，只做最小修复，不要重构。
7. 每次修改后必须给出运行方式和小白验证步骤。
8. 最后必须生成 README、演示脚本、项目亮点、提交检查清单。

## 推荐协作流程

### 1. 规划阶段

使用 MVP Planner：

- 拆题；
- 判断必做功能；
- 放弃不必要功能；
- 确定 Demo 主线。

### 2. 架构阶段

使用 Hackathon Architect：

- 确定技术栈；
- 设计目录结构；
- 拆页面和组件；
- 确定数据结构。

### 3. 开发阶段

使用 Fast Builder：

- 实现主页面；
- 实现核心交互；
- 接入 mock 数据；
- 确保项目能运行。

### 4. 修复阶段

使用 Build Fixer：

- 处理启动失败；
- 处理页面白屏；
- 处理终端报错；
- 处理按钮无反应。

### 5. 包装阶段

使用 Demo Coach：

- 写 README；
- 写 30 秒介绍；
- 写 2 分钟演示脚本；
- 总结项目亮点；
- 准备缺陷解释。

## 技术选择建议

默认优先选择：

- React + Vite；
- Tailwind CSS；
- mock 数据；
- localStorage；
- 单页应用；
- 无复杂后端。

除非赛题明确要求后端、数据库、AI API，否则不要主动引入复杂服务。

## 验收标准

一个功能完成，必须满足：

1. 页面上能看到入口；
2. 用户能点击或输入；
3. 点击后有反馈；
4. 成功或失败都有提示；
5. 用户能肉眼判断功能是否生效；
6. 评委现场能理解这个功能有什么价值。

## 禁止事项

1. 不要为了炫技做复杂架构。
2. 不要一次性生成太多文件。
3. 不要在主流程没跑通前做动画、美化、重构。
4. 不要报错后乱改。
5. 不要最后才写 README。
6. 不要只说“测试通过”，必须告诉用户怎么看。
