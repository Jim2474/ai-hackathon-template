# Moodwave MVP 架构设计

## 推荐技术栈

| 技术 | 解释 | 为什么适合黑客松 |
|------|------|-----------------|
| **React + Vite** | 前端框架 + 构建工具 | 启动快，代码简单，模板现成 |
| **Tailwind CSS** | 样式工具 | 写样式像拼积木，不用想太多 |
| **p5.js** | 绘图库 | 做粒子效果最简单，10 分钟就能出效果 |
| **mock 数据** | 假数据 | 不用连网，不用写后端，直接用本地数据 |

一句话总结：只做前端，不用后端，所有数据都是本地写好的假数据。

## 最小目录结构

```
ai-hackathon-template/
├── public/                 # 静态资源（图片、音频等，暂时不用）
├── src/
│   ├── components/         # 组件文件夹
│   │   ├── InputArea.jsx   # 输入区组件
│   │   ├── PlanDisplay.jsx # 方案展示组件
│   │   └── ParticleCanvas.jsx # 粒子效果组件
│   ├── data/               # 数据文件夹
│   │   └── mockPlans.js    # 假数据（AI 生成的方案）
│   ├── App.jsx             # 主应用组件
│   ├── main.jsx            # 入口文件
│   └── index.css           # 全局样式
├── index.html
├── package.json
└── vite.config.js
```

## 核心数据模型

### 1. 用户输入
```javascript
{
  text: "我现在很困，但还要学 2 小时高数"
}
```

### 2. AI 生成的方案
```javascript
{
  mode: "focus",
  modeName: "专注模式",
  duration: 120,
  explanation: "考虑到你需要学习...",
  timeline: [
    "0-30分钟：轻柔钢琴，帮助进入状态",
    "30-90分钟：中等节奏，保持专注",
    "90-120分钟：渐慢，准备休息"
  ],
  hasVoiceReminder: false,
  autoFadeOut: true
}
```

### 3. 应用状态
```javascript
{
  phase: "idle",
  userInput: "",
  currentPlan: null,
  particleMode: "floating"
}
```

## 页面与组件设计

### App.jsx - 主应用（指挥官）
**负责什么：** 管理整个应用的状态，像指挥官一样协调各个组件。
- 记住用户输入了什么
- 记住当前在哪个阶段（输入中、思考中、看方案、播放中）
- 把数据传递给各个组件

### InputArea.jsx - 输入区（前台接待）
**负责什么：** 让用户输入文字，点击按钮。
- 显示输入框
- 显示几个快速选择按钮（学习、睡觉、运动、安抚）
- 用户点击「生成方案」后，告诉 App 要开始思考了

### PlanDisplay.jsx - 方案展示区（讲解员）
**负责什么：** 把 AI 生成的方案展示给用户看。
- 显示模式标签（专注模式）
- 显示时长
- 显示方案说明
- 显示播放计划
- 显示「开始播放」按钮

### ParticleCanvas.jsx - 粒子背景（气氛组）
**负责什么：** 全屏粒子效果，根据状态变化。
- 空闲时：粒子缓慢漂浮
- 思考中：粒子向中心聚合
- 播放中：粒子根据模式变化（环形、脉冲等）

### mockPlans.js - 假数据仓库（剧本库）
**负责什么：** 存几套固定的方案，假装是 AI 生成的。
- 专注模式方案
- 睡眠模式方案
- 安抚模式方案
- 运动模式方案
- 白噪音模式方案

## 主要函数/API 设计

### generatePlan(userInput) - 生成方案（假装是 AI）
**输入：** 用户说的话
**输出：** 一个方案对象
**做什么：** 从 mock 数据里随机挑一个方案返回

### setPhase(phase) - 切换阶段
**输入：** 阶段名称（idle/thinking/plan/playing）
**做什么：** 更新应用状态，改变粒子效果

### startPlayback() - 开始播放
**做什么：** 切换到播放状态，粒子效果变化

## 开发顺序

**第 1 步：初始化项目 + 创建文件结构**
- 先把项目跑起来
- 创建上面说的那些空文件

**第 2 步：写 mock 数据**
- 写 5 套固定的方案数据

**第 3 步：做基础 UI（InputArea + PlanDisplay）**
- 不用粒子，先把输入和展示做出来
- 用假数据测试流程

**第 4 步：做粒子效果**
- 加入 p5.js
- 实现基础粒子漂浮
- 实现状态变化

**第 5 步：连起来 + 美化**
- 把所有组件连起来
- 调整颜色和样式

## 小白验收方式

**第 1 步完成后：**
- 运行项目，浏览器能打开页面
- 虽然页面是空的，但不会报错

**第 2 步完成后：**
- 打开代码文件，能看到 5 套方案数据
- 就像写了 5 个剧本

**第 3 步完成后：**
- 能在输入框里打字
- 点击按钮，能看到一个方案显示出来
- 流程能跑通，虽然没有粒子效果

**第 4 步完成后：**
- 页面背景有粒子在飘
- 点击不同按钮，粒子会变化

**第 5 步完成后：**
- 整个流程顺畅
- 看起来很酷
- 能给别人演示

## 风险与简化方案

| 风险 | 简化方案 |
|------|---------|
| p5.js 不会用 | 不用粒子了，用 CSS 动画代替，效果差一点但能演示 |
| 时间不够 | 只保留输入、展示 2 个核心功能，砍掉粒子 |
| mock 数据写起来麻烦 | 只写 2-3 套方案就行，不用写 5 套 |

## 第一步应该做什么

**让 Builder 先做：初始化项目，把基础文件结构创建好，确保项目能跑起来。**

具体来说：
1. 检查当前项目是否已经有 React + Vite 配置
2. 如果没有，初始化一个
3. 创建我们上面说的目录结构
4. 创建那些空文件
5. 运行 `npm install` 和 `npm run dev`，确保能打开页面
